const axios = require('axios');

async function run() {
  try {
    // login sandhyamajhi77@gmail.com
    // what is the password? 
    // actually, let's login as np05... wait, I don't know the password
  } catch (e) {}
}
